var coral = new Coral({
  class: 'slider-first'
});

// $('#remove').click(() => {
//   $('.coral-slider').find('.coral-item').first().remove();
//   const leftOffset = Math.ceil($('.coral-slider').find('.coral-item').first().offset().left);
//   $('.coral-slider').css('transform', `translate3d(${leftOffset + 560}px, 0px, 0px)`);
//   // $('.coral-slider').css('transition', `transform ${this.speed}s`);
// });